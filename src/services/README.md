# Services

## Appwrite

1) Copy `appwrite.secrets.example.ts` to `appwrite.secrets.ts`
2) Fill `APPWRITE_ENDPOINT`, `APPWRITE_PROJECT_ID`, `APPWRITE_DATABASE_ID`, and collection IDs

The real `appwrite.secrets.ts` is ignored by git.
